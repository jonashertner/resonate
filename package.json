{
  "name": "resonate",
  "private": true,
  "type": "module",
  "description": "A personal atlas. The app itself has no dependencies and no build step; these exist only so a browser can be driven in CI.",
  "scripts": {
    "test": "node --test test/*.test.mjs club/test/*.test.mjs",
    "test:browser": "playwright test",
    "serve": "node tools/dev.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
